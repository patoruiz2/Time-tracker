/** Stub — Phase 5 */
export const parseVoiceTranscript = (transcript: string) => {
  void transcript;
  return {
  title: '',
  adoId: undefined as string | undefined,
  description: undefined as string | undefined,
  };
};
